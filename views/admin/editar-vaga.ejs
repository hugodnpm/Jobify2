<%- include('header') %> <!-- incluindo o cabeçalho(outro arquivo) nesta pagina -->
    
        
    
    <h2>JOBIFY - ADMINISTRADOR - EDITAR VAGA:
        <a href="/admin/vagas"> Vaga</a>
    </h2>
    
    <div>
        <form method="POST">
            <label>Título:</label>
            <input type="text" name="titulo" value="<%= vaga.titulo %>">
            <br>
            <label>Descrição:</label>
            <textarea name="descrição"><%= vaga.descrição %>"></textarea>
            <br>
            <label>Categoria:</label>
            <select name="categoria" >
                <%categorias.forEach( categoria => { %>
                <option value="<%= categoria.id %>" <% if(categoria.id == vaga.categoria){ %>selected <% } %>><%= categoria.categoria %></option>
                
                <% }) %>
            </select>
            <br>
            <button>Salvar Vaga</button>
        </form>
        
    </div>
    
    
    <%- include('footer') %>